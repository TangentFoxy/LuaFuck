Tac: also known as cat; but backwards
>    move right so we have a beginng NULL (will be an ending NULL)
     (Note: In LuaFuck this would not be needed; this is for best compatibility)
,    input to allow entering loop
[>,] go right and read until we get a NULL
+    plus so we can enter another loop
[<.] go left and output (til we reach that starting NULL of course)

[uneeded, just prettifies the output (when running from terminal) by putting an extra \n
 (also note that none of the code will be run that's in here,
  because it is in a loop immediately following a loop)
 +>.><--+., Woo!
]

++++++++++. 10 is a linefeed; aka enter
